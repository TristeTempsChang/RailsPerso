<div id="modalDelete" class="hidden fixed inset-0 bg-black bg-opacity-50 overflow-y-auto h-full w-full">
    <div class="absolute top-1/2 left-1/2 transform -translate-x-1/2 -translate-y-1/2 mx-auto p-5 border w-96 shadow-lg rounded-md bg-white">
        <div class="flex justify-end">
            <button id="closeModalDel" class="text-black">X</button>
        </div>
        <h1 class="text-center font-semibold text-lg mb-4">Delete article</h1>
        <p class="text-center">Are you sure you want to delete?</p>
        <div class="flex items-center justify-center">
            <%= submit_tag "Delete", class: "bg-green-600 text-white p-3 rounded-lg", id: "delete-submit" %>
        </div>
    </div>
</div>


<%= javascript_include_tag 'modal' %>